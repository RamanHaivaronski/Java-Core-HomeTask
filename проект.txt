2 interfaces
1 abstract class
1 inner class
1 class extends abstract and implements 1 interface
1 classs extends abstract implements 2 interfaces
exceptions

volume + area

2D 

circle, (center (x y) , boundary (x y))
Area = Pi*r*r

equilateral triangle, (x1 y1 x2 y2 x3 y3)
Area = root(s(s-x)(s-y)(s-z))
s=(z+x+y)/2

square ( x1 y2 x2 y2 )
Area = a*a

3D Pyramid (one base area)

cone
Area = Pi*r*r + Pi*r*L
(L - slant height)
Volume = 1/3*Pi*r*r*H
(H - height)

equilateral triangular pyramid,
Volume = h*a*a/4*(root(3))
(H - height)
Area = (root(3))/4*a*a + 3*0.5*a*L
(L - slant height)

square pyramid
Volume = 1/3*h*a*a
(H - height)
Area = a*a + 2*a*L
(L - slant height)

3D Prism (base and top areas)

cylinder
Area = 2*Pi*r(h+r)
Volume = Pi*r*r*h

equilateral triangular prism
Volume = (root(3))/4*a*a*h
Area = 2*(root(3))/4*a*a + 3*a*h
(H - height)

square prism (9) 
Volume = a*a*h
Area = 4*a*h + 2*a*a
 
2D 3D

Methods: 

getDistance() radius of circle 
getDistance() length of the side of triangle
getDistance() side of the square

dhfdskhfkdsf
